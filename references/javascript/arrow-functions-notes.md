# Arrow functions source notes

- **Source:** Author's Arrow functions notes supplied on September 14, 2026.
- **Order:** The fat arrow, return without return, one parameter, multiple parameters, block body, practice, and recap.
- **Equivalence:** Show `(x, y) => x + y` stored in a const next to the same `add` declaration.
- **Return:** Compare `square` as a declaration with the one-line arrow version.
- **Parentheses:** Optional for one parameter, required for several. Add the zero-parameter case and one SyntaxError example.
- **Block body:** Keep the `addAndLog` example. Add one example where braces without return give undefined.
- **Practice:** Use the author's exercise: `divide`, `whisper`, and `shorterThan`, with one quiz on braces without return.
- **Out of scope:** Scope and events are separate later lessons and are not covered here.
