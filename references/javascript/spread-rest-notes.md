# Spread and rest source notes

- **Source:** Author's Spread slides and the spread operator and rest parameter notes supplied on September 15, 2026.
- **Order:** What spread is, combining arrays, passing arguments, spreading objects, rest parameter, spread or rest, practice, and recap.
- **Examples:** Slide snippets (`numbers`, `burnBook` with the `concat` equivalent, `skills.push(...newSkills)`) and the notes' `person`, `sum`, and `logDetails` snippets are kept.
- **Added:** The without-the-dots contrast for `numbers` and `push`, a copy example for "clone", the override line `{ ...person, age: 31 }`, a snippet that uses spread and rest together, and a comparison table.
- **Layout:** Every code box has a neutral lead above it and the explanation bullets below it, so readers see the code before the names it introduces.
- **Out of scope:** Destructuring with rest, spreading strings into arrays, and spreading into Math.max beyond the practice box.
