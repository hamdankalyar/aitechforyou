# map, filter, and reduce source notes

- **Source:** Author's map & filter slides and the "Map, Filter, and Reduce Explained with Real-World Examples" notes supplied on September 15, 2026.
- **Order:** Process all items (sandwich figure), map, string templates, filter, reduce, chaining, practice, and recap.
- **Examples:** Slide snippets (`spices` with `nicknames` and `mels`, the `${s.nickname} Spice` equivalence) and the notes' `cart` doubling, `affordableProducts`, and `totalPrice` snippets are kept. Every number was checked in Node: 168.96 and 38.98 print exactly.
- **Figure:** The sandwich diagram is not on the page and is not described. The chain section reproduces its filter, map, reduce order in code.
- **Added:** A general string template example, a reduce example without a starting value to show the note "if we do not give the value of the acc then its initial value is the first value of array", including the object surprise, the chain section, and a comparison table.
- **Out of scope:** find, some, every, and sort with a compare function.
