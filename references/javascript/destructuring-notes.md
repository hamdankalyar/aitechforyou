# JavaScript destructuring source notes

- **Source:** Author's destructuring notes supplied on September 18, 2026.
- **Objects:** Show taking named properties into new variables.
- **Arrays:** Show taking items by position.
- **Property names:** Show that `PI` matches `Math.PI` and lowercase `pi` gives `undefined`.
- **Values:** Show that changing a destructured number does not change `Math.PI`.
- **Arrays:** Show skipped positions and collecting remaining items with `...`.
