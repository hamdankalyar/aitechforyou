# JavaScript error handling source notes

- **Source:** Author's error handling notes supplied on September 18, 2026.
- **try and catch:** Show that errors inside `try` skip remaining lines and jump to `catch`.
- **throw:** Show `throw new Error(message)` for an error created by the program.
- **Error details:** Cover `name`, `message`, and the browser-dependent `stack` text.
- **finally:** Explain that it runs after `try` and `catch` whether an error occurred or not.
